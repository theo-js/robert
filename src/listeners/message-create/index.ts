import type { ClientListener } from '../type';

export const onMessageCreate: ClientListener = (client) => {
    // client.on('messageCreate', (message) => {
    //     console.log(message.content);
    // });
};
